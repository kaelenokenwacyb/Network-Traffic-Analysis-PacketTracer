# Network-Traffic-Analysis
This project demonstrates my guided experience analyzing network traffic using Wireshark. I captured and examined packets to understand how common protocols operate and how data flows across a network.

Overview
- This project showcases my experience using Packet Tracer to simulate and analyze network behavior. Through guided labs, I
configured and tested network components to better undetstand how devices communicate across local and wide area networks

Objectives:
- Understand IP addressing and subnetting across multiple networks
- Configure and observe DHCP-based IP assignment
- Analyze client-server communication using DNS, TCP, and HTTP
- Explore routing between multiple networks using routers
- Test and verify connectivity using ICMP (ping)

Tools Used:
- Cisco Packet Tracer

Screenshot #1 (DHCP Configuration):
- This screenshot demonstrates how a client devices obtains an IP address dynamically using DHCP.
Observations:
- Client successfully receives an IP address, subnet mask, and default gateway
- DHCP communication occurs between the client and the server
- Packet flow can be analyzed using the OSI model view

Screenshot #2 (Client-Server Communication):
- This screenshot shows how a client accesses a web server through domain name resolution and web protocols.
Observations:
- DNS resolves domain names into IP addresses
- TCP establishes a connection between client and server
- HTTP is used to load web content
- Multiple protocols work together to enable communication

Screenshot #3 (Multi-Router Network):
- This screenshot demonstrates communication across multiple networks connected by routers.
Observations:
- Routers maintain routing tables to direct traffic
- Multiple subnets are configured with unique IP ranges
- Traffic is forwarded based on destination network

Screenshot #4 (Switching vs. Routing / LAN-to-LAN):
- This screenshot simulates the comparison between communication within a local network versus across different networks.
Observations:
- Switches handle communication with a LAN
- Routers manage traffic between different networks
- ICMP packets are used to test connectivity

Screenshot #5 (Network Connectivity Testing):
- This screenshots demonstrates a successful communication between devices using ICMP ping commands.
Observations:
- Devices successfully send and receive ICMP echo requests
- Connectivity between networks is verified
- No packet loss indicates proper network configuration

Key Takeaways:
- Developed a foundational understanding of network configuration and communication
- Gained experience with routing, switching, and subnetting concepts
- Learned how multiple protocols interact in real-world networking scenarios
- Practices troubleshooting and verifying network connectivity
